{
  "name": "basic-server",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc --outDir dist",
    "test": "mocha -r ts-node/register/transpile-only --exit 'test/**/*.ts'",
    "lint": "echo lint",
    "clean": "echo clean"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.1.1",
    "ts-node": "^8.8.1",
    "typescript": "^3.8.3"
  }
}
