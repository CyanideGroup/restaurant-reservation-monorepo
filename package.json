{
  "name": "restaurant-reservation-monorepo",
  "version": "0.0.1",
  "main": "index.js",
  "repository": "git@github.com:CyanideGroup/restaurant-reservation-monorepo.git",
  "author": "JustynaBroniszewska <jusbroniszewska@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "wsrun": "^5.2.0"
  },
  "workspaces": [
    "basic-server"
  ],
  "scripts": {
    "build": "yarn wsrun --stages --fast-exit build",
    "test": "yarn wsrun --serial --fast-exit test",
    "lint": "yarn wsrun --stages lint",
    "clean": "yarn wsrun clean"
  }
}
